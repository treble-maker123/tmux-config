# split panes using | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# reload config file
bind r source-file ~/.tmux.conf

# enable mouse control
set -g mouse on

# enable/disable mouse mode
bind m \
       set -g mouse on \;\
       display 'Mouse: ON'
       bind M \
         set -g mouse off \;\
         display 'Mouse: OFF'

# status bar settings
         set -g status-justify centre # center the windows
         set -g status-left '#[fg=green]#H'
         set -g status-right 'CPU: #{cpu_percentage} GPU: #{gpu_percentage} #[fg=blue]%Y-%m-%d #[fg=blue,bg=default]%a%l:%M:%S %p#[UPTIME]'
###############################################################################
# Plugins
###############################################################################

# List of plugins
         set -g @plugin 'tmux-plugins/tpm'
         set -g @plugin 'tmux-plugins/tmux-sensible'
         set -g @plugin 'tmux-plugins/tmux-cpu'

         set -g @plugin 'seebi/tmux-colors-solarized'
         set -g @colors-solarized 'dark'

# set -g @plugin 'tmux-plugins/tmux-resurrect
# set -g @plugin 'tmux-plugins/tmux-continuum'
# set -g @continuum-restore 'on' # last saved environments is automatically started

# >>> should keep this as last line >>>
         run -b '~/.tmux/plugins/tpm/tpm'
# <<< should keep this as last line <<<
